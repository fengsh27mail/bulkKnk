






# 3. 网络动力学传播计算 (迭代求解平衡态)
for (i in 1:steps) {
  E_new <- alpha * (W_current %*% E_current) + (1 - alpha) * E_init
  E_current <- E_new
}

message(">> 网络传播计算收敛，已达到最终稳态。")

# 【Bug 修复】：保留基因名称
final_expr <- as.vector(E_current)
names(final_expr) <- names(E_init) 

return(final_expr)
}
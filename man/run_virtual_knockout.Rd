% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_virtual_knockout.R
\name{run_virtual_knockout}
\alias{run_virtual_knockout}
\title{执行全网络虚拟基因或通路干预 (核心网络扰动引擎)}
\usage{
run_virtual_knockout(
  target_genes = NULL,
  E_init,
  adj_matrix,
  alpha = 0.7,
  steps = 30,
  check_convergence = FALSE
)
}
\arguments{
\item{target_genes}{字符向量。支持单个或多个靶点 (如 c("GeneA", "GeneB"))。}

\item{E_init}{数值型向量。全网基因的基线表达量 (通常为目标群体的均值)。}

\item{adj_matrix}{矩阵。经过清洗和归一化的有向调控网络。}

\item{alpha}{数值。网络信息保留系数 (restart probability)，默认 0.7。}

\item{steps}{整数。迭代寻找稳态的次数，默认 30 次。}

\item{check_convergence}{逻辑值。是否打印每步收敛误差，默认 FALSE。}
}
\value{
返回一个数值型向量，代表虚拟干预后的全网稳态绝对表达量。
}
\description{
执行全网络虚拟基因或通路干预 (核心网络扰动引擎)
}

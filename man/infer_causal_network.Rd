% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/infer_causal_network.R
\name{infer_causal_network}
\alias{infer_causal_network}
\title{推断因果调控网络并进行动态去噪}
\usage{
infer_causal_network(
  expr_matrix,
  threshold_method = "top_quantile",
  q_val = 0.95,
  sd_multiplier = 3,
  n_cores = 1
)
}
\arguments{
\item{expr_matrix}{表达量矩阵。格式要求：行是样本 (Samples)，列是基因 (Genes)。}

\item{threshold_method}{动态阈值策略，可选 "top_quantile" (默认) 或 "mean_sd"。}

\item{q_val}{当使用 "top_quantile" 时，设定的分位数阈值，默认 0.95 (即 Top 5\%)。}

\item{sd_multiplier}{当使用 "mean_sd" 时，标准差的倍数，默认 3。}

\item{n_cores}{并行计算的线程数，默认 1 (如果是庞大网络建议设置更大)。}
}
\value{
返回一个 List，包含清洗后的权重矩阵、阈值信息和保留的连线数。
}
\description{
本函数利用 GENIE3 算法推断基因间的有向调控关系，并自动基于数据分布特征
（分位数法或均值标准差法）动态设定硬阈值，过滤背景噪音，输出干净的邻接矩阵。
}
\examples{
# 假设你已经有了 simulated_data
# network_res <- infer_causal_network(simulated_data, threshold_method = "top_quantile")
}
